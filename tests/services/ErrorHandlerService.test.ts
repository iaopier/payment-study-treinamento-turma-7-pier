import { describe, it, expect } from 'vitest'; import { ErrorHandlerService } from '../../src/services/ErrorHandlerService'; describe('ErrorHandlerService', () => { it('should return 503 for ConnectionError', () => { const error = new Error('Connection failed'); error.name = 'ConnectionError'; const result = ErrorHandlerService.handleInfrastructureError(error); expect(result.statusCode).toBe(503); }); it('should return 500 for generic error', () => { const result = ErrorHandlerService.handleInfrastructureError(new Error('Generic')); expect(result.statusCode).toBe(500); }); });