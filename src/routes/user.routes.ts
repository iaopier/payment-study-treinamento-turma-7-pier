import { Router } from 'express'; import { UserController } from '../controllers/UserController'; const router = Router(); const controller = new UserController(); router.get('/users/:id/activity-report', controller.getActivityReport); export default router;