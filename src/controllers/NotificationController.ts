import { Request, Response } from 'express'; import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); export class NotificationController { async listByUser(req: Request, res: Response) { const { userId } = req.params; const { isRead, page = '1', limit = '10' } = req.query; const skip = (Number(page) - 1) * Number(limit); const notifications = await prisma.notification.findMany({ where: { userId, isRead: isRead === 'true' ? true : undefined }, skip, take: Number(limit), orderBy: { createdAt: 'desc' } }); return res.json(notifications); } async markAsRead(req: Request, res: Response) { const { id } = req.params; await prisma.notification.update({ where: { id }, data: { isRead: true } }); return res.status(204).send(); } async markAllAsRead(req: Request, res: Response) { const { userId } = req.params; await prisma.notification.updateMany({ where: { userId, isRead: false }, data: { isRead: true } }); return res.status(204).send(); } }