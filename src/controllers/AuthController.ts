import { FastifyReply, FastifyRequest } from 'fastify'; import { authService } from '../services/AuthService'; export const authController = { async register(req: FastifyRequest, reply: FastifyReply) { const user = await authService.register(req.body as any); return reply.status(201).send(user); }, async login(req: FastifyRequest, reply: FastifyReply) { const { email, password } = req.body as any; try { const token = await authService.login(email, password); return reply.send({ token }); } catch { return reply.status(401).send({ message: 'Invalid credentials' }); } } };