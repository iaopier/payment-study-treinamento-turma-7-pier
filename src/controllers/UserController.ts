import { Request, Response } from 'express'; import { NotificationService } from '../services/NotificationService'; const service = new NotificationService(); export const getNotifications = async (req: Request, res: Response) => { const userId = (req as any).user.id; const { page, limit, readStatus } = req.query; const data = await service.listNotifications(userId, Number(page) || 1, Number(limit) || 10, readStatus === 'true' ? true : readStatus === 'false' ? false : undefined); res.json(data); }; export const markRead = async (req: Request, res: Response) => { const userId = (req as any).user.id; await service.markAsRead(req.params.id, userId); res.status(204).send(); }; export const markAllRead = async (req: Request, res: Response) => { const userId = (req as any).user.id; await service.markAllAsRead(userId); res.status(204).send(); };