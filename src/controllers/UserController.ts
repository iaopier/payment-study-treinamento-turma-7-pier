import { FastifyReply, FastifyRequest } from 'fastify'; import { z } from 'zod'; import { AuthService } from '../services/AuthService'; import { prisma } from '../lib/prisma'; const registerSchema = z.object({ email: z.string().email(), password: z.string().min(6) }); export class UserController { static async register(req: FastifyRequest, reply: FastifyReply) { const { email, password } = registerSchema.parse(req.body); const hashedPassword = await AuthService.hashPassword(password); const user = await prisma.user.create({ data: { email, password: hashedPassword } }); return reply.status(201).send({ id: user.id }); } static async login(req: FastifyRequest, reply: FastifyReply) { const { email, password } = registerSchema.parse(req.body); const user = await prisma.user.findUnique({ where: { email } }); if (!user || !(await AuthService.comparePassword(password, user.password))) return reply.status(401).send({ error: 'Invalid credentials' }); const token = AuthService.generateToken(user.id); return reply.send({ token }); } }