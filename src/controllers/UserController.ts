import { FastifyRequest, FastifyReply } from 'fastify'; import { UpdateUserService } from '../services/UpdateUserService'; export class UserController { constructor(private updateUserService: UpdateUserService) {} async update(request: FastifyRequest<{ Params: { id: string }, Body: { name: string } }>, reply: FastifyReply) { const { id } = request.params; const { name } = request.body; const user = await this.updateUserService.execute({ id, name }); return reply.status(200).send(user); } }