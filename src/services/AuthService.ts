import jwt from 'jsonwebtoken'; import bcrypt from 'bcryptjs'; export const AuthService = { generateToken: (userId: string) => jwt.sign({ userId }, process.env.JWT_SECRET || 'secret', { expiresIn: '1h' }), verifyToken: (token: string) => jwt.verify(token, process.env.JWT_SECRET || 'secret'), hashPassword: async (password: string) => await bcrypt.hash(password, 12), comparePassword: async (password: string, hash: string) => await bcrypt.compare(password, hash) };