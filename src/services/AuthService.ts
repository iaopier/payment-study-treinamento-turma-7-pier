import bcrypt from 'bcrypt'; import { generateToken } from '../utils/jwt'; import { userRepository } from '../repositories/UserRepository'; export const authService = { async register(data: any) { const hashedPassword = await bcrypt.hash(data.password, 10); return await userRepository.create({ ...data, password: hashedPassword }); }, async login(email: string, password: string) { const user = await userRepository.findByEmail(email); if (!user || !(await bcrypt.compare(password, user.password))) throw new Error('Unauthorized'); return generateToken({ id: user.id, email: user.email }); } };