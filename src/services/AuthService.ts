import jwt from 'jsonwebtoken'; import bcrypt from 'bcrypt'; const JWT_SECRET = process.env.JWT_SECRET || 'secret'; export class AuthService { static async generateToken(userId: string): Promise<string> { return jwt.sign({ userId }, JWT_SECRET, { expiresIn: '24h' }); } static async verifyToken(token: string): Promise<any> { return jwt.verify(token, JWT_SECRET); } static async comparePasswords(password: string, hash: string): Promise<boolean> { return bcrypt.compare(password, hash); } }