import jwt from 'jsonwebtoken'; import bcrypt from 'bcrypt'; import { prisma } from '../lib/prisma'; const SECRET = process.env.JWT_SECRET || 'secret'; export class AuthService { async authenticate(email: string, password: string) { const user = await prisma.user.findUnique({ where: { email } }); if (!user || !(await bcrypt.compare(password, user.password))) return null; return jwt.sign({ id: user.id }, SECRET, { expiresIn: '1h' }); } verify(token: string) { try { return jwt.verify(token, SECRET); } catch { return null; } } }