import { prisma } from '../lib/prisma'; export class UserService { async createUser(data: any) { return await prisma.user.create({ data }); } async findById(id: string) { return await prisma.user.findUnique({ where: { id } }); } }