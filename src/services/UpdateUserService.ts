import { UserRepository } from '../repositories/UserRepository'; export class UpdateUserService { private userRepository: UserRepository; constructor() { this.userRepository = new UserRepository(); } async execute(id: string, data: any) { const userExists = await this.userRepository.findById(id); if (!userExists) { throw new Error('User not found'); } return await this.userRepository.update(id, data); } }