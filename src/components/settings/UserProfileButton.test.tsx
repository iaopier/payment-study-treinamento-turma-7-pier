import { render, screen, fireEvent } from '@testing-library/react'; import { describe, it, expect, vi } from 'vitest'; import { UserProfileButton } from './UserProfileButton'; describe('UserProfileButton', () => { it('deve renderizar o botão com o label correto', () => { render(<UserProfileButton label="Acessar Conta" onClick={() => {}} />); expect(screen.getByText('Acessar Conta')).toBeDefined(); }); it('deve chamar a função onClick ao ser clicado', () => { const handleClick = vi.fn(); render(<UserProfileButton label="Acessar" onClick={handleClick} />); fireEvent.click(screen.getByText('Acessar')); expect(handleClick).toHaveBeenCalledTimes(1); }); });