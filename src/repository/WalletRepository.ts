import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); export class WalletRepository { async getUnreadCount(userId: string): Promise<number> { try { const count = await prisma.notification.count({ where: { userId, read: false } }); return count; } catch (error) { console.error('Erro ao consultar notificações:', error); throw new Error('Falha ao buscar contador de notificações'); } } }