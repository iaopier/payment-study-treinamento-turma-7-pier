generator client { provider = "prisma-client-js" } datasource db { provider = "postgresql" url = env("DATABASE_URL") } model UserActivityReport { id String @id @default(uuid()) accountId String createdAt DateTime @default(now()) startDate DateTime endDate DateTime data Json @@index([accountId, startDate, endDate]) }