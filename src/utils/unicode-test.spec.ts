import { describe, it, expect } from 'vitest'; import { normalizeUnicode, isUnicodeSupported, countUnicodeCharacters } from './unicode-test'; describe('unicode-test utils', () => { it('should normalize unicode strings', () => { const input = 'u\u0308'; expect(normalizeUnicode(input)).toBe('ü'); }); it('should detect unicode characters', () => { expect(isUnicodeSupported('abc')).toBe(false); expect(isUnicodeSupported('ü')).toBe(true); }); it('should count characters correctly including surrogate pairs', () => { expect(countUnicodeCharacters('abc')).toBe(3); expect(countUnicodeCharacters('😊')).toBe(1); }); });